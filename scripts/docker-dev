#!/usr/bin/env bash
# Back-compat wrapper — prefer `docker-dev` after install.sh
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export DOCKER_DEV_REPO="${DOCKER_DEV_REPO:-$ROOT}"
exec "${ROOT}/cli/bin/docker-dev" "$@"